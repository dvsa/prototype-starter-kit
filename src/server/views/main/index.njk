{% extends 'layouts/default.layout.njk' %}

{% set pageTitle = 'Prototype Starter Kit' %}

{% block content %}

  {{ components.pageHeading('Prototype Starter Kit', 'Build v' + repoPackageJSON.version) }}

  {% call components.gridRow() %}

    {% call components.columnOneThird() %}

      {{ components.heading(text='Styleguide', tag='h2', size='medium', url='/library') }}
      {{ components.paragraph(text='GDS and Custom components library to be used by all applications.') }}
      
    {%- endcall %}

    {% call components.columnOneThird() %}

      {{ components.heading(text='Prototypes', tag='h2', size='medium', url='/prototypes') }}
      {{ components.paragraph(text='All prototypes and user journeys.') }}
      
    {%- endcall %}

  {%- endcall %}

{% endblock %}